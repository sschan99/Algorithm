#include <iostream>
#include <set>
using namespace std;

int main() {
	int n, x;
	cin >> n;
	set<int> s;
	while (n--) {
		cin >> x;
		if (s.count(-x)) {
			s.erase(-x);
		}
		else {
			s.insert(x);
		}
	}
	int result = 0;
	for (int a : s) {
		result += a;
	}
	cout << result;
	return 0;
}